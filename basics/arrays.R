# Arrays are objects which can store data in more than two dimensions
# array(data, dim, dimnames)
arr = array(c(1:9), dim=c(3,3,4,2))
# dim = c(nrows, ncols, xdim, ydim) xdim * ydim = number of mini matrices that are created